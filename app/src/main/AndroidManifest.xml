<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.numad21fa_jeannillehiciano">
<!-- A5 allow location -->
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


<!-- https://developer.android.com/training/basics/network-ops/connecting -->
<!-- A6 - request INTERNET permission for access to internet -->
    <uses-permission android:name="android.permission.INTERNET"/>
<!--  request permission to perform network operations in app -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_jh"
        android:label="NUMAD21Fa_JeannilleHiciano"
        android:roundIcon="@mipmap/ic_launcher_jh_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21Fa_JeannilleHiciano"
        tools:ignore="ExtraText">
        <activity
            android:name=".WebViewActivity"
            android:exported="true" />
        <activity
            android:name=".WebService"
            android:exported="true" />
        <activity
            android:name=".LocationActivity"
            android:exported="true" />
        <activity
            android:name=".RViewHolder"
            android:configChanges="orientation|screenSize"
            android:exported="true" />
        <activity
            android:name=".AboutMe"
            android:exported="true" />
        <activity
            android:name=".GridButtonActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>